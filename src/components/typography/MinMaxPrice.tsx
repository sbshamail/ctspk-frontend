import React from "react";

interface MinMaxPriceProps {
  max_price: number;
  min_price: number;
}
const MinMaxPrice = ({ max_price, min_price }: MinMaxPriceProps) => {
  return <div></div>;
};

export default MinMaxPrice;
